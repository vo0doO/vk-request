import json
import pandas as pd
import numpy as np
import pickle
import io
def pick_data_file(file):
    """
    # st = io.BytesIO(open(file, "rb").read())
    #dt = pd.read_pickle(file, compression='infer')
    #data = pickle.load(st);
    #st.close()
    #return data
    # for user in dt:
    #     user = pd.DataFrame.from_dict(user)
    #     data = pd.DataFrame.co
    # store = pd.HDFStore("test.h5")
    # store["girl"] = data
    # data = store["girl"]
    # store.close()
    # html = pd.DataFrame.to_html(data)
    # try:
    #    pantkmc = pd.DataFrame.to_panel(data)
    #    json_data = pd.DataFrame.to_json(data, "pdDF.json", orient="split")
    #    xarr = pd.DataFrame.to_xarray(data)
    """

data = pd.DataFrame.from_dict()